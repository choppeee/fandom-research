/** 이 두 문장은 어디에 쓰이든 다시 쓰지 않는다 - 랜딩/로그인/About/리포트/PDF 어디서든
 * 같은 문장을 그대로 가져다 쓴다(문구가 표면마다 조금씩 달라지는 걸 막기 위함). */

// 외부에 노출되는 유일한 브랜드 문장. 기능(댓글/분석/AI/리서치)을 설명하지 않는다.
export const BRAND_MANIFESTO = "흩어진 신호를, 결정 가능한 근거로.";

// 내부 전용 - Hero 등 사용자 대면 화면에 직접 노출하지 않는다. 카피를 쓸 때 방향을 판단하는
// 기준으로만 쓴다(이 문장을 "표현"하는 카피인지 자문할 때 참고).
export const BRAND_MISSION_INTERNAL = "우리는 사람들이 더 잘 결정하도록 돕습니다.";
